# FileForge 사용자 매뉴얼

> 실무자용 설명서입니다. 프로그램을 처음 받은 분이 이 문서만 읽고 바로 쓸 수 있도록 만들었습니다.

---

## 1. 이 프로그램은 무엇인가요?

**FileForge**는 폴더 안에 쌓인 **파일과 폴더의 이름을 한 번에 바꾸고, 정리하고, 새 폴더를 한꺼번에 만들고, PDF·엑셀·이미지를 변환**해 주는 프로그램입니다.
파일 100개의 이름을 하나씩 고치던 일, 부서별·월별 폴더를 손으로 하나씩 만들던 일을 몇 번의 클릭으로 끝냅니다.

가장 중요한 두 가지 성질이 있습니다.

1. **실행하기 전에 결과를 미리 보여줍니다.** "이 파일이 이런 이름으로 바뀝니다", "이런 폴더가 만들어집니다"를 눈으로 확인한 뒤에 실행합니다.
2. **실행한 뒤에도 되돌릴 수 있습니다.** 화면 아래 **실행 취소** 버튼을 누르면 직전 작업이 통째로 원래대로 돌아갑니다.

> 인터넷으로 자료를 보내지 않습니다. 모든 처리는 내 컴퓨터 안에서만 이루어집니다.

---

## 2. 실행 방법

### 윈도우

1. `파일명일괄변경_윈도우용.pyw` 파일을 **더블클릭**합니다.
   (Python이 설치돼 있어야 합니다. 없다면 [python.org](https://www.python.org/downloads/)에서 설치 — 설치 화면에서 **"Add Python to PATH"**에 체크하세요.)
2. 잠시 뒤 **기본 웹브라우저**가 저절로 열리면서 프로그램 화면이 나타납니다.
3. Python 설치 없이 쓰고 싶다면 `EXE만들기.bat`으로 만든 `FileForge.exe`를 더블클릭하세요.

### 종료 방법

**브라우저 탭을 닫으면 프로그램도 함께 완전히 종료됩니다.** 따로 끌 것이 없습니다.
(탭을 닫고 1분 정도 지나면 백그라운드 프로그램도 스스로 사라집니다.)

### 화면 구성

```
┌──────────────────────────────────────────────────────────────┐
│ FileForge — 파일 자동화 도구      자동저장됨 · 14:32  입력 초기화 │ ← ① 상태 표시줄
├──────────────────────────────────────────────────────────────┤
│ FILEFORGE   이름 변경 | 폴더 정리 | 폴더 만들기 | PDF·변환      │ ← ② 기능 탭
│                            [확장자 필터]  [ 폴더 선택 ]        │ ← ③ 폴더 고르기
├──────────────────────────────────────────────────────────────┤
│  FORGE EVERY FILE.                                           │
│  [폴더 선택하기]  C:\작업\2026자료 · 파일 32개 · 폴더 4개        │ ← ④ 지금 보고 있는 폴더
├──────────────────────────────────────────────────────────────┤
│                                                              │
│   (고른 탭에 따라 규칙·옵션이 나옵니다)                          │ ← ⑤ 작업 영역
│                                                              │
│   Preview — 현재 이름 → 변경 후 · 상태                          │ ← ⑥ 미리보기
│                                                              │
├──────────────────────────────────────────────────────────────┤
│ 파일 32개 · 적용 대상 30개   [폴더 열기][실행 취소][변경 실행]    │ ← ⑦ 아래 고정 막대
└──────────────────────────────────────────────────────────────┘
```

### 기본 사용 순서 (3단계)

1. 오른쪽 위 **폴더 선택** 을 눌러 작업할 폴더를 고릅니다.
2. 위쪽 탭에서 하고 싶은 일을 고르고, 옵션을 켭니다. → **미리보기가 바로 바뀝니다.**
3. 아래쪽 **변경 실행** 을 누릅니다. (키보드로는 `Ctrl` + `Enter`)

---

## 3. 기능별 사용법

### 3-1. 이름 변경 탭 — 파일과 **폴더** 이름을 한 번에 바꾸기

#### ① 무엇을 하는 기능인가요?

폴더 안에 있는 파일 이름을, 정해 둔 규칙에 따라 한꺼번에 바꿉니다.
**파일뿐 아니라 폴더 이름도 똑같은 규칙으로 바꿀 수 있습니다.**

#### ② 어떻게 쓰나요?

**1단계 — 무엇을 바꿀지 고릅니다 (변경 대상)**

규칙 목록 바로 위에 **변경 대상** 선택칸이 있습니다.

| 고르는 값 | 무엇이 바뀌나요 |
|---|---|
| **파일만** | 폴더는 손대지 않고 파일 이름만 바꿉니다. (기본값) |
| **폴더만** | 파일은 손대지 않고 폴더 이름만 바꿉니다. |
| **파일 + 폴더** | 둘 다 한꺼번에 바꿉니다. |

> - 폴더는 **확장자가 없기 때문에 이름 전체가 바뀝니다.** `2023.연말자료` 처럼 이름에 점(.)이 있어도 `.연말자료`를 확장자로 착각하지 않습니다.
> - 미리보기에서 폴더는 앞에 **📁** 표시가 붙어 파일과 구분됩니다.
> - **폴더만**을 고르면 오른쪽 위 확장자 필터는 회색으로 꺼집니다. (폴더에는 확장자가 없으니까요)

**2단계 — 규칙을 켭니다**

검은 알약 모양 **칩**을 누르면 규칙이 켜지고(검게 반전), 아래에 입력칸이 나타납니다. 여러 개를 동시에 켤 수 있고, **위에서 아래 순서대로** 적용됩니다.

| 규칙 | 하는 일 | 예 |
|---|---|---|
| 이름 새로 입력 | 기존 이름을 지우고 통째로 새 이름을 넣습니다 (순번 / 공통 이름 / 파일별 직접 입력) | `아무이름.jpg` → `001.jpg` |
| 키워드별 지정값으로 변경 | 이름이 왼쪽 값과 **똑같으면** 오른쪽 값으로 바꿉니다 | `1.pdf` → `1_이력서.pdf` |
| 찾기 / 바꾸기 | 특정 글자를 다른 글자로 (정규식도 가능) | `임시` → `최종` |
| 자동 정리 | 연속 공백 정리, 특수문자 제거, 공백을 밑줄(_)로 | `보고서   (1).xlsx` → `보고서 1.xlsx` |
| 접두사 / 접미사 | 이름 앞뒤에 같은 글자 붙이기 | `보고서.xlsx` → `[영업팀] 보고서_최종.xlsx` |
| 파일별 텍스트 | 파일마다 **다른** 글자를 앞이나 뒤에 붙이기 | 아래 설명 참고 |
| 날짜 붙이기 | 오늘 날짜 또는 파일 수정일 붙이기 | `보고서.xlsx` → `20260819_보고서.xlsx` |
| 순번 매기기 | 001, 002 … 번호 붙이기 | `사진.jpg` → `01_사진.jpg` |
| 대소문자 | 영문 대/소문자 통일, 확장자 소문자로 | `Photo.JPG` → `photo.jpg` |

**3단계 — 미리보기를 확인합니다**

| 상태 표시 | 뜻 |
|---|---|
| 변경 (초록) | 이대로 바뀝니다 |
| 변경 없음 | 바뀔 내용이 없습니다 |
| ⚠ 중복 (빨강) | 결과 이름이 서로 겹칩니다 → **자동으로 제외**됩니다 |
| ⚠ 이미 존재 (빨강) | 같은 이름이 폴더에 이미 있습니다 → **자동으로 제외**됩니다 |
| ⚠ 사용불가 | 이름에 쓸 수 없는 글자(`\ / : * ? " < > |`)가 들어갔습니다 |
| 제외 | 사용자가 직접 뺀 항목입니다 |

- **행을 클릭**하면 포함/제외가 바뀝니다. 행 위를 **드래그**하면 여러 줄을 한 번에 포함/제외할 수 있습니다.
- 왼쪽 **⠿** 손잡이를 잡고 끌면 순서를 바꿀 수 있습니다. (순번 매기기에 그대로 반영됩니다)

**4단계 — 실행**

아래 **변경 실행**(`Ctrl`+`Enter`) → 확인 창에서 "파일 12개 · 폴더 3개에 적용합니다" 를 확인 → **변경 실행**.

#### ③ 결과는 어떻게 나오나요?

화면 아래에 `12개 이름 변경 완료 · 실행 취소 가능` 처럼 표시되고, 목록이 새 이름으로 갱신됩니다.
잘못됐으면 바로 **실행 취소**(`Ctrl`+`Z`)를 누르면 전부 원래 이름으로 돌아갑니다.

#### 💡 사용 예 — 폴더 이름 앞에 번호 붙이기

부서 폴더 4개(`영업팀`, `관리팀`, `인사팀`, `기획팀`)에 순서 번호를 붙이고 싶을 때:

1. 폴더 선택 → 변경 대상을 **폴더만** 으로
2. **순번 매기기** 칩을 켜고 → 시작 1, 자릿수 2, 위치 **앞에**
3. 미리보기에서 순서가 마음에 안 들면 ⠿ 손잡이로 드래그해 순서 조정
4. 변경 실행 → `01_영업팀`, `02_관리팀`, `03_인사팀`, `04_기획팀`

---

### 3-2. 폴더 정리 탭 — 흩어진 파일을 하위 폴더로 모으기

#### ① 무엇을 하는 기능인가요?

폴더 안에 뒤섞인 파일들을 기준에 따라 **하위 폴더를 만들어 그리로 옮깁니다.**

#### ② 어떻게 쓰나요?

1. 폴더를 선택합니다.
2. 정리 기준을 고릅니다.

| 기준 | 만들어지는 폴더 |
|---|---|
| 카테고리별 | `문서`, `엑셀·데이터`, `이미지`, `영상`, `압축` … |
| 확장자별 | `PDF`, `XLSX`, `JPG` … |
| 날짜별 (년-월) | `2026-08` |
| 날짜별 (년-월-일) | `2026-08-19` |
| 키워드별 | 입력한 키워드가 **파일 이름에 들어 있는** 파일끼리 그 키워드 폴더로 |

3. **폴더 구조 미리보기**에서 어떤 폴더에 몇 개가 들어가는지 확인합니다. (`신규` / `기존` 표시)
4. **정리 실행**을 누릅니다.

> **키워드별** 을 고르면 입력칸이 나타납니다. `＋ 키워드 추가` 를 클릭하면 칸이 1개 늘고, **누른 채로 아래로 끌면 여러 칸이 한꺼번에** 생깁니다.
> 여러 키워드에 걸리는 파일은 **위쪽 키워드**가 우선이고, 어떤 키워드에도 안 맞는 파일은 그 자리에 그대로 둡니다.

#### ③ 결과는?

`7개 이동 완료 · 실행 취소 가능`. 되돌리면 파일이 원래 자리로 돌아가고, 그때 만들어진 빈 폴더도 지워집니다.

---

### 3-3. 폴더 만들기 탭 — 원하는 폴더를 한꺼번에 만들기 ⭐신규

#### ① 무엇을 하는 기능인가요?

부서별·월별·프로젝트별 폴더를 손으로 하나씩 만들 필요 없이, **원하는 이름의 폴더를 한 번에 수십·수백 개** 만듭니다.
각 폴더 안에 들어갈 **하위 폴더까지 똑같이** 만들어 줍니다.

#### ② 어떻게 쓰나요?

먼저 오른쪽 위 **폴더 선택** 으로 "이 안에 새 폴더를 만들 위치"를 지정합니다.
그다음 만드는 방식을 칩에서 고릅니다. **5가지** 방식이 있습니다.

**① 목록으로 만들기** — 이름을 직접 적기

한 줄에 폴더 이름을 하나씩 적습니다.

```
영업팀
관리팀
인사팀
```

- **엑셀에서 이름이 적힌 열을 복사해 그대로 붙여넣어도 됩니다.**
- `/` 를 쓰면 하위 폴더까지 한 번에 만듭니다. → `2026/01월` 이라고 적으면 `2026` 폴더 안에 `01월` 폴더가 생깁니다.
- 아래 버튼: **예시로 채우기**(연습용 샘플) · **중복 줄 정리**(같은 이름 제거) · **붙여넣기** · **비우기**

**② 번호로 만들기** — 같은 이름 + 일련번호

`앞에 붙일 이름` + `시작 번호` + `자릿수` + `개수` 를 정합니다.
예) 앞이름 `사진_`, 시작 1, 자릿수 3, 개수 4 → `사진_001`, `사진_002`, `사진_003`, `사진_004`

**③ 날짜로 만들기** — 기간을 정하면 날짜 폴더가 쭉

시작일·종료일을 고르고, `하루마다` / `일주일마다` / `한 달마다` 중에서 고릅니다.
예) 2026-01-01 ~ 2026-04-30, 한 달마다, 형식 `2026년 01월` → `2026년 01월` ~ `2026년 04월`

**④ 조합으로 만들기 (A × B)** — 두 목록을 곱하기

A 목록에 `영업팀`, `관리팀` / B 목록에 `1월`, `2월` 을 넣으면 4개가 만들어집니다.

| 방식 | 결과 |
|---|---|
| A 폴더 안에 B 폴더 | `영업팀/1월`, `영업팀/2월`, `관리팀/1월`, `관리팀/2월` |
| 한 폴더 이름으로 합치기 | `영업팀_1월`, `영업팀_2월`, `관리팀_1월`, `관리팀_2월` |

**⑤ 파일 이름으로 만들기** — 폴더 안 파일에서 이름을 가져오기

이미 폴더에 들어 있는 파일 이름을 그대로 폴더 이름으로 씁니다.

| 폴더 이름 기준 | 설명 | 예 (`2024_홍길동_계약서.pdf`) |
|---|---|---|
| 파일 이름 전체 | 확장자만 뗀 이름 | `2024_홍길동_계약서` |
| 기호 앞부분까지 | 정한 기호(기본 `_`) 앞까지만 | `2024` |
| 앞에서 N글자 | 앞 N글자만 | (4글자) `2024` |

같은 이름으로 묶이는 파일들은 **한 폴더로 모입니다.** 아래 **"만든 폴더로 파일도 함께 옮기기"**에 체크하면 폴더를 만들면서 파일까지 그 안으로 옮겨줍니다.

**공통 옵션 (모든 방식에 적용)**

| 옵션 | 설명 |
|---|---|
| 상위 폴더 | 여기에 이름을 적으면 그 폴더를 먼저 만들고 **그 안에** 나머지를 만듭니다. 비우면 선택한 폴더 바로 아래에 만듭니다. |
| 하위 폴더 공통 생성 | 만들어지는 **모든 폴더 안에** 같은 하위 폴더를 만듭니다. 쉼표로 구분해 적습니다. 예) `01_원본, 02_작업, 03_완료` |

#### ③ 결과는 어떻게 나오나요?

아래 **만들어질 폴더 미리보기**에 트리 그림으로 표시됩니다.

- `신규` = 새로 만들어짐 / `이미 있음` = 이미 있으므로 **건드리지 않고 건너뜀** (안에 든 파일은 절대 지워지지 않습니다)
- 요약: `총 13개 폴더 생성 · 이미 있는 1개는 건너뜀`

**폴더 만들기**(`Ctrl`+`Enter`)를 누르면 확인 창이 뜨고, 실행 후 `폴더 13개 생성 완료 · 실행 취소 가능` 이 표시됩니다.
**실행 취소**를 누르면 방금 만든 폴더만 깔끔히 사라집니다. (옮긴 파일도 함께 원래 자리로 돌아갑니다.)

> 한 번에 최대 **500개**까지 만듭니다. 그보다 많으면 안내가 뜨고 500개까지만 실행되니, 나눠서 실행하세요.

#### 💡 사용 예 — 부서별 × 월별 폴더를 하위 폴더까지 한 번에

3개 부서 × 12개월 폴더를 만들고, 각 폴더마다 `01_원본`, `02_최종` 을 넣고 싶을 때:

1. 폴더 선택 → **폴더 만들기** 탭 → **조합으로 만들기**
2. A 목록에 `영업팀 / 관리팀 / 인사팀`, B 목록에 `1월` … `12월` 을 한 줄씩
3. 방식은 **A 폴더 안에 B 폴더**
4. 공통 옵션 → 상위 폴더 `2026년 자료`, 하위 폴더 공통 생성 `01_원본, 02_최종`
5. 미리보기 확인 → **폴더 만들기**

결과: `2026년 자료/영업팀/1월/01_원본` … 총 100개가 넘는 폴더가 한 번에 만들어집니다.

---

### 3-4. PDF · 변환 탭

#### ① 무엇을 하는 기능인가요?

PDF를 합치고 나누고 줄이거나, 이미지·엑셀·한글 문서를 다른 형식으로 바꿉니다. **원본 파일은 그대로 두고 새 파일로 저장합니다.**

#### ② 어떻게 쓰나요?

1. 폴더를 선택합니다.
2. 위쪽 칩에서 작업을 고릅니다. → 그 작업에 맞는 파일만 목록에 나옵니다.
3. 목록에서 동그라미를 눌러 파일을 선택합니다. (전체 선택 / 전체 해제 버튼 있음)
4. **실행**(`Ctrl`+`Enter`)을 누릅니다.

| 작업 | 설명 |
|---|---|
| PDF 병합 | 선택한 PDF들을 하나로 합침. 파일마다 오른쪽 칸에 `1-5, 7-8` 처럼 적으면 **그 페이지만** 합칩니다 (비우면 전체) |
| PDF 분할 | PDF 1개를 **N페이지마다** 잘라 새 폴더에 저장. 또는 `페이지 삭제` 칸에 `1-3, 5` 를 적으면 그 페이지를 뺀 새 PDF를 만듭니다 |

> 💡 스캐너나 한글(HWP)에서 만든 PDF는 파일 내부 구조가 깨져 있는 경우가 있습니다. 이런 파일도 **자동으로 복구해서** 처리하며, 결과에 복구했다는 안내가 함께 표시됩니다. **원본 파일은 바뀌지 않습니다.**

| 이미지 → PDF | 여러 장의 스캔 이미지를 한 PDF로 묶기 |
| PDF → 이미지 | 각 페이지를 JPG/PNG로 저장 (`이름_1`, `이름_2` …) |
| 이미지 형식 변환 | PNG·HEIC·TIFF·PDF 등을 JPG/PNG로 일괄 변환 |
| PDF 용량 줄이기 | 이메일 첨부용으로 크기 축소 → `_압축.pdf` |
| 엑셀 → PDF | 엑셀 파일들을 각각 PDF로 (LibreOffice 필요) |
| 한글 → PDF | .hwp/.hwpx를 PDF로 (한글 프로그램 또는 LibreOffice 사용) |
| 엑셀 시트 합치기 | 여러 엑셀의 **모든 시트**를 한 파일로 모으기 |
| 엑셀 행 통합 | 여러 엑셀의 행을 위에서 아래로 이어 붙여 한 시트로 |

#### ③ 결과는?

`✓ PDF 5개(총 84페이지)를 합쳤습니다 → 병합_20260819_1432.pdf` 처럼 만들어진 파일 이름이 표시됩니다.
여러 건을 처리할 때 **일부가 실패해도 나머지는 계속 진행**되고, 실패한 파일과 이유가 아래에 정리돼 나옵니다.

---

### 3-5. 어디서나 쓸 수 있는 편의 기능

| 기능 | 설명 |
|---|---|
| **자동저장** | 입력한 규칙·목록은 **자동으로 저장**됩니다. 화면 맨 위에 `자동저장됨 · 14:32` 로 마지막 저장 시각이 보입니다. |
| **이어서 하기** | 실수로 창을 닫았다가 다시 열면 *"이전 작업 내용이 있습니다. 이어서 하시겠습니까?"* 라고 물어봅니다. **이어서 하기** 를 누르면 규칙·입력값이 그대로 복원됩니다. |
| **마지막 폴더 기억** | 다시 실행하면 지난번에 쓰던 폴더·확장자 필터·탭이 그대로 열립니다. |
| **입력 초기화** | 화면 오른쪽 위 버튼. 저장된 입력값을 모두 비웁니다. (**파일은 전혀 바뀌지 않습니다.**) |
| **폴더 열기** | 아래 막대의 버튼. 지금 보고 있는 폴더를 탐색기(Finder)로 바로 엽니다. |
| **최근 작업 이력** | 화면 맨 아래 `최근 작업 이력` 을 펼치면 언제 무엇을 했는지 최근 30건이 보입니다. (이 컴퓨터에만 저장) |
| **확장자 필터** | 오른쪽 위 칸에 `jpg,png` 처럼 적으면 그 파일만 목록에 나옵니다. |

### 단축키

| 키 | 하는 일 |
|---|---|
| `Ctrl` + `Enter` | 실행 (확인 창이 떠 있으면 확인) |
| `Ctrl` + `Z` | 실행 취소 |
| `Esc` | 확인 창 닫기 |
| `Enter` | (키워드별 지정값 입력칸에서) 아래에 줄 추가 |

> 맥에서는 `Ctrl` 대신 `⌘(Command)` 도 됩니다.

---

## 4. 자주 묻는 질문 · 문제 해결

| 증상 | 원인 | 해결 방법 |
|---|---|---|
| 폴더를 선택했는데 목록이 비어 있음 | 확장자 필터가 걸려 있음 | 오른쪽 위 확장자 필터 칸을 비우세요. |
| **폴더만** 을 골랐는데 아무것도 안 나옴 | 그 폴더 안에 하위 폴더가 없음 | 하위 폴더가 있는 폴더를 선택하세요. |
| 확장자 필터 칸이 회색으로 눌리지 않음 | 변경 대상이 **폴더만** 으로 되어 있음 | 정상입니다. 폴더는 확장자가 없어 필터가 필요 없습니다. |
| `⚠ 이미 존재` 라고 뜨며 제외됨 | 바꾸려는 이름이 이미 폴더에 있음 | 접두사·순번 등을 더해 이름이 겹치지 않게 하세요. 덮어쓰기를 막기 위한 안전장치입니다. |
| `⚠ 중복` 이 뜸 | 여러 파일의 결과 이름이 서로 같아짐 | **순번 매기기** 규칙을 함께 켜서 이름을 구분해 주세요. |
| 폴더 이름 바꾸기가 실패함 | 그 폴더나 안의 파일이 다른 프로그램에서 열려 있음 | 탐색기 창, 엑셀·한글 등 그 폴더를 쓰고 있는 프로그램을 모두 닫고 다시 시도하세요. |
| 만들려는 폴더가 `이미 있음` 으로 표시됨 | 같은 이름의 폴더가 이미 존재 | 정상입니다. 기존 폴더와 그 안의 파일은 **건드리지 않고 건너뜁니다.** |
| `같은 이름의 파일이 이미 있어 폴더를 만들 수 없습니다` | 만들려는 폴더 이름과 똑같은 **파일**이 있음 | 폴더 이름을 바꾸거나, 그 파일 이름을 먼저 바꾸세요. |
| 500개까지만 만들어짐 | 한 번에 만들 수 있는 최대 개수 | 안내 문구대로 기간·개수를 나눠서 두 번 실행하세요. |
| 실행 취소를 눌렀는데 폴더가 안 지워짐 | 그 폴더 안에 다른 파일이 들어감 | 비어 있는 폴더만 지웁니다. 내용물이 생긴 폴더는 안전을 위해 남겨 둡니다. 직접 확인 후 지우세요. |
| 붙여넣기 버튼이 동작하지 않음 | 브라우저가 클립보드 접근을 막음 | 입력칸을 클릭한 뒤 `Ctrl`+`V` 로 직접 붙여넣으세요. |
| 엑셀 → PDF 가 안 됨 | LibreOffice 미설치 | [libreoffice.org](https://www.libreoffice.org/download) 에서 무료 설치 후 다시 시도하세요. |
| PDF 기능이 처음에 느림 | 필요한 부품(pypdf·Pillow)을 자동 설치 중 | 처음 한 번만 그렇습니다. 잠시 기다리세요. |
| PDF 분할에서 `전체 0페이지뿐이라…` 라고 뜸 | PDF 파일 내부의 목차 정보가 깨져 페이지를 못 읽던 문제 | **수정되었습니다.** 이제 이런 파일은 자동으로 복구해서 처리하고, 결과에 *"자동 복구 후 처리했습니다"* 라고 표시됩니다. (원본 파일은 그대로 둡니다) |
| `열기 암호가 걸린 PDF입니다` | 파일을 열 때 암호를 물어보는 PDF | 그 PDF를 열어 암호를 입력한 뒤 **다른 이름으로 저장**해서 암호 없는 파일로 만들어 다시 시도하세요. |
| `PDF를 읽을 수 없습니다` | 파일이 심하게 손상됐거나 이름만 .pdf 인 파일 | 크롬에서 그 파일을 열어 **인쇄 → PDF로 저장** 으로 새로 만든 뒤 다시 시도하세요. |
| 프로그램을 어떻게 끄나요? | — | 브라우저 탭을 닫으면 됩니다. 백그라운드 프로그램도 함께 종료됩니다. |
| 자료가 외부로 나가나요? | — | 나가지 않습니다. 서버는 내 컴퓨터(127.0.0.1) 안에서만 동작합니다. |

---

## 5. 안전에 대해

- **원본을 덮어쓰지 않습니다.** 이름이 겹치는 상황은 미리 감지해 자동으로 제외합니다.
- **모든 작업은 실행 취소로 되돌릴 수 있습니다.** (여러 번 누르면 차례로 되돌아갑니다)
- **폴더 만들기는 기존 폴더를 절대 지우거나 덮어쓰지 않습니다.** 이미 있으면 건너뜁니다.
- PDF·변환 작업은 원본을 그대로 두고 **새 파일**을 만듭니다.
- 자료는 인터넷으로 전송되지 않습니다.
